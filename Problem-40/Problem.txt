
40.)Create a C# program that asks the user for two integers (x, y) and shows the range of those numbers (the two included), in three different ways:

    1.Using the for statement.
    2.Using the while statement.
    3.Using the do while instruction.